{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug current folder (lldb)",
      "type": "cppdbg",
      "request": "launch",
      "MIMode": "lldb",
      "program": "${fileDirname}/main",
      "cwd": "${fileDirname}",
      "args": [],
      "externalConsole": true,
      "stopAtEntry": false,
      "preLaunchTask": "Build (clang++) current folder"
    }
  ]
}
